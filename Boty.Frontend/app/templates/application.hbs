<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
      data-toggle="collapse" data-target="#menu-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}
      <img src='/icon.png' width=22 height=22 style='display:inline;'/> Boty
      {{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="menu-collapse">
      <ul class="nav navbar-nav">
          {{#if session.isAuthenticated}}
            {{#if session.data.authenticated.isAdmin}}
              <li>{{ link-to 'Accounts' 'accounts'}}</li>
            {{/if}}
            {{#if session.data.authenticated.isDeviceMaker }}
              <li>{{ link-to 'Devices' 'serial'}}</li>
            {{/if}}
            <li>{{ link-to 'Node Plugins' 'node-plugins'}}</li>
          {{/if}}

        <li>{{ link-to 'About' 'about' }}</li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
          {{#if session.isAuthenticated}}
            <li><a href="#" {{ action 'invalidateSession' on='click' }} ><i class="fa fa-sign-out"/> Logout</a></li>
          {{else}}
            <li>{{#link-to 'log-in' }}<i class="fa fa-sign-in"/> Log-In{{/link-to}}</li>
          {{/if}}
      </ul>
    </div>
  </div>
</nav>

<div class="container" style="margin-bottom: 40px">
    {{outlet}}
</div>
<footer class="navbar navbar-default navbar-fixed-bottom footer">
  <p class="navbar-text">&#169; The Boty Team (2016)</p>
</footer>
<!-- vim: set tabstop=2 shiftwidth=2 expandtab: -->
